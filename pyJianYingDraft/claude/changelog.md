# 变更记录

## 2026-06-24 09:19:30 — 初始化模块索引

- 生成 `pyJianYingDraft/CLAUDE.md`（轻量索引）+ `claude/` 详情。
- 信息源：`__init__.py` 全文、`setup.py`、17 个 `.py` 与 `metadata/` 路径。
- 跳过：各片段/元数据文件的函数体细节。
- 下一步：深挖 `video_segment.py`/`text_segment.py` 参数与导出结构、`metadata/` 参数注释。
